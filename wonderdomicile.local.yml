# Local config for testing patterns

driver:
  typename: simpixel
  num: 2400

# enables numpy. currently v slow
#numbers: float

aliases:
  bpa: BiblioPixelAnimations.matrix

layout:
  typename: matrix
  width: 16
  height: 150
  brightness: 255

path: ./animations/

animation:
  typename: sequence
  length: 10
  run:
    fps: 30
    threaded: true
  animations:
    - typename: chase.Chase
      name: Chase
      run:
        fps: 20
      palette:
        colors: rainbow
    - typename: triangles.Triangles
      name: BlinkyTriangles
      run:
        fps: 20
      palette:
        colors: rainbow
      spacing: 0
      group_spacing: 10
      blink: true
      fade: 0.9
    - typename: triangles.Triangles
      name: ScrollingTriangles
      run:
        fps: 20
      palette:
        colors: rainbow
    - typename: spiral.Spiral
      name: Spiral
      run:
        fps: 20
      palette:
        colors: rainbow
    - typename: colorwave.Horizontal
      palette:
        colors: rainbow
    #- typename: multiprocessing_test.MultiProcessingTest
    #- typename: $bpa.MatrixRain.MatrixRain
    #  run:
    #    fps: 10
    #- typename: $bpa.bloom.Bloom
    #  run:
    #    fps: 15
    #- typename: $bpa.Twinkle.Twinkle
    #  run:
    #    fps: 10
    #- typename: $bpa.pinwheel.Pinwheel
    #- typename: $bpa.GameOfLife.GameOfLifeRGB
    #- typename: $bpa.SpinningTriangle.SpinningTriangle
