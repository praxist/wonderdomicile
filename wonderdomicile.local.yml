# Local config for testing patterns

driver:
  typename: simpixel
  num: 400

# enables numpy. currently v slow
#numbers: float

aliases:
  bpa: BiblioPixelAnimations.matrix

path: ./animations/


animation:

  # typename: chase.ChaseChris
  # name: Wat
  # run:
  #   fps: 60
  # palette:
  #   colors: rainbow
  # spacing: 100
  # lllength: 10
  # fade: .9
  # dirrrection: 1

  typename: strips.Mega
  name: Wat
  run:
    fps: 60
  palette:
    colors: rainbow

  # typename: strips.Bump
  # name: Wat
  # run:
  #   fps: 100
  # palette:
  #   colors: rainbow
  # bpm: 80
  # multiple: 1

layout:
  typename: matrix
  width: 200
  height: 2
  brightness: 255
